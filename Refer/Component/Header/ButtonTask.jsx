import React from 'react';
import PropTypes from 'prop-types';

ButtonTask.propTypes = {
    
};

function ButtonTask(props) {
    return (
        <div>
            <button type="button" className="btn__search">Create New Task</button>
        </div>
    );
}

export default ButtonTask;